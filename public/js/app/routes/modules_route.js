(function() {
  define(['ember', 'jquery', '../../shared/xhr/parse_link_header'], function(Ember, $, parseLinkHeader) {
    var ModulesRoute;
    return ModulesRoute = Ember.Route.extend();
  });

}).call(this);
