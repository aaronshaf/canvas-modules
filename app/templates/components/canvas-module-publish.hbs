<span class="module-published-button no-wrap" {{action 'togglePublish'}}>
  Published
  {{#if published}}
    <i class="icon-publish" aria-hidden="true"></i>
  {{else}}
    <i class="icon-unpublished"></i>
  {{/if}}
</span>